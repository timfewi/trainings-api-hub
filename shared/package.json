{
  "name": "@trainings-api-hub/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for API Sandbox",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
